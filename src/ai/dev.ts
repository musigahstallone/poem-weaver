/**
 * @fileOverview Genkit Development UI Server.
 *
 * This file is typically used to start the Genkit Developer UI,
 * which allows for inspecting and testing flows locally.
 * It re-exports the configured `ai` instance from `genkit.ts`.
 */
import './genkit'; // Ensures genkit.ts runs and registers plugins/flows
